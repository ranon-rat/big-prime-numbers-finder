# big prime numbers

This is a very simple project that I made mostly to test some of my capabilities.

To run it you only need to compile it running the build.py and then executing the binary generated inside the folder `.moonmake/bin/prime-generator`.

After that it will generate you a bunch of primes inside the folder `primes`
